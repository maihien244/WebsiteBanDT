{{#if (equal numberPages 0)}}
  <nav aria-label="Page navigation"></nav>
{{else}}
    <nav aria-label="Page navigation">
      <ul class="pagination">
        {{{prePage currentPage}}}
        {{{renderPagination numberPages currentPage}}}
        {{{nextPage currentPage numberPages}}}
      </ul>
    </nav>
{{/if}}